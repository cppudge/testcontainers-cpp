# Build & install
/build/
/install/
/out/

# Docs-site build outputs (mkdocs.yml / docs/doxygen/Doxyfile)
/site/
/docs/site/api/
/test_package/build/
/packaging/conan-center/recipes/testcontainers-cpp/all/test_package/build/
compile_commands.json

# Conan (project-local home + generated files)
/.conan2/
CMakeUserPresets.json
conan_toolchain.cmake
conandata.yml.lock

# Editor / IDE
/.vs/
/.idea/
*.user

# clangd background-index cache (regenerated on demand; stale entries can cause
# phantom diagnostics — delete this folder to force a clean re-index)
/.cache/

# Reference-only nested repos (external sources / previous FFI fork) — not part of this project
/testcontainers-cxx/
/_research/
